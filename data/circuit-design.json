{
  "title": "ThumbJoy 3-Wire Bridge: ATtiny85 I2C Circuit Design",
  "version": "1.0",
  "date": "2026-03-25",
  "status": "READY FOR PROTOTYPE",
  "overview": "A tiny circuit board (~8mm × 6mm) inside the ThumbJoy grip that reads the dual Melexis Hall sensor analog outputs, digitizes them with a 10-bit ADC, and sends the data down to the Teensy 4.1 over I2C — using only 3 wires through the HF55 hollow shaft (VCC + GND + SDA/SCL on shared wire pair).",

  "schematic": {
    "gripSide": {
      "title": "GRIP PCB (inside top handle housing)",
      "mcu": {
        "part": "ATtiny85V-10SU",
        "package": "SOIC-8 (5.3mm × 4mm body)",
        "voltage": "2.7V–5.5V (runs on 5V from shaft)",
        "clock": "8 MHz internal (no external crystal needed)",
        "digikey": "ATTINY85V-10SU-ND",
        "cost": "$1.23",
        "pinout": [
          {"pin": 1, "name": "PB5/RESET", "function": "RESET (tie to VCC via 10kΩ)", "connection": "10kΩ pull-up to VCC"},
          {"pin": 2, "name": "PB3/ADC3", "function": "Melexis Channel 1 (analog input)", "connection": "→ Melexis OUT1"},
          {"pin": 3, "name": "PB4/ADC2", "function": "Melexis Channel 2 (analog input)", "connection": "→ Melexis OUT2"},
          {"pin": 4, "name": "GND", "function": "Ground", "connection": "→ GND (shaft wire 2)"},
          {"pin": 5, "name": "PB0/SDA", "function": "I2C SDA (USI data)", "connection": "→ SDA (shaft wire 3)"},
          {"pin": 6, "name": "PB1/DO", "function": "Not used (LED debug optional)", "connection": "NC or debug LED"},
          {"pin": 7, "name": "PB2/SCL", "function": "I2C SCL (USI clock)", "connection": "→ SCL (shared with SDA on 3-wire)"},
          {"pin": 8, "name": "VCC", "function": "Power", "connection": "→ VCC (shaft wire 1)"}
        ]
      },
      "passives": [
        {"part": "C1: 100nF ceramic (0402)", "function": "Decoupling on VCC pin", "placement": "As close to pin 8 as possible"},
        {"part": "C2: 10µF tantalum (0805)", "function": "Bulk decoupling", "placement": "Near power entry"},
        {"part": "R1: 10kΩ (0402)", "function": "RESET pull-up", "placement": "Pin 1 to VCC"},
        {"part": "R2: 4.7kΩ (0402)", "function": "I2C SDA pull-up", "placement": "Pin 5 to VCC"},
        {"part": "R3: 4.7kΩ (0402)", "function": "I2C SCL pull-up", "placement": "Pin 7 to VCC"}
      ],
      "totalBOM": "$1.85",
      "boardDimensions": "8mm × 6mm, 2-layer, 0.8mm thick",
      "notes": "Board mounts flat next to the Melexis bracket inside the grip housing. Can be secured with a dab of hot glue or double-sided tape. Keep away from Z-axis magnet zone."
    },
    "shaftWiring": {
      "title": "THROUGH-SHAFT WIRING (3 wires)",
      "wires": [
        {"wire": 1, "color": "Red", "signal": "VCC (+5V)", "gauge": "30 AWG", "notes": "Powers ATtiny85 + Melexis sensor. ~25mA total draw."},
        {"wire": 2, "color": "Black", "signal": "GND", "gauge": "30 AWG", "notes": "Common ground return."},
        {"wire": 3, "color": "Yellow", "signal": "SDA (I2C data)", "gauge": "30 AWG", "notes": "Bidirectional I2C data. Pulled up to VCC by 4.7kΩ on grip PCB."}
      ],
      "i2cNote": "Standard I2C uses 2 data wires (SDA + SCL). However, at this short distance (~10-15cm shaft) and low speed (100kHz), we can use the ATtiny85's USI in TWI mode which can operate with the SCL generated by the master and only SDA running through the shaft. The Teensy 4.1 generates SCL — both sides see it. Total: 3 wires.",
      "alternative3Wire": "If true 2-wire I2C (SDA+SCL) is needed, run SCL as a 4th wire OR use software UART instead: ATtiny TX → Teensy RX = only 1 data wire. UART is simpler firmware and more noise-tolerant.",
      "recommendedProtocol": "UART (Software Serial) — simplest, only needs TX pin, Teensy reads on any digital pin via SoftwareSerial or hardware Serial port. 3 wires total: VCC + GND + TX."
    },
    "teensySide": {
      "title": "TEENSY 4.1 (in base housing — existing hardware)",
      "connection": "Wire 3 (SDA/TX) connects to any available I2C or Serial RX pin on the Teensy 4.1",
      "i2cPins": "Teensy 4.1 I2C: Wire (pins 18/SDA, 19/SCL) or Wire1 (pins 17/SDA1, 16/SCL1) or Wire2 (pins 25/SDA2, 24/SCL2)",
      "uartPins": "Teensy 4.1 Serial: Serial1 (pin 0/RX1), Serial2 (pin 7/RX2), Serial3 (pin 15/RX3), etc.",
      "notes": "Choose whichever bus is free on the existing ThumbJoy PCB v3. If all I2C buses are in use (display, etc.), UART is cleanest."
    }
  },

  "firmware": {
    "gripFirmware": {
      "title": "ATtiny85 Firmware (Grip Side)",
      "language": "Arduino / C",
      "library": "TinyWireS (for I2C slave) OR SoftwareSerial (for UART)",
      "code": "// === ThumbJoy 3-Wire Bridge — ATtiny85 Grip Firmware ===\n// Reads 2 Melexis analog channels, sends via I2C to Teensy\n// I2C Slave address: 0x27\n// Sends 4 bytes: [CH1_HIGH, CH1_LOW, CH2_HIGH, CH2_LOW]\n\n#include <TinyWireS.h>\n\n#define I2C_ADDR 0x27\n#define MEL_CH1  A3   // PB3, pin 2 — Melexis output 1\n#define MEL_CH2  A2   // PB4, pin 3 — Melexis output 2\n\nvolatile uint16_t ch1_val = 512;\nvolatile uint16_t ch2_val = 512;\n\nvoid setup() {\n  pinMode(MEL_CH1, INPUT);\n  pinMode(MEL_CH2, INPUT);\n  TinyWireS.begin(I2C_ADDR);\n  TinyWireS.onRequest(requestEvent);\n}\n\nvoid loop() {\n  // Read both Melexis channels (10-bit ADC, 0–1023)\n  ch1_val = analogRead(MEL_CH1);\n  ch2_val = analogRead(MEL_CH2);\n  TinyWireS_stop_check();  // Required for TinyWireS\n  delay(5);  // ~200 Hz sample rate\n}\n\n// Called when Teensy requests data\nvoid requestEvent() {\n  uint8_t buf[4];\n  buf[0] = (ch1_val >> 8) & 0xFF;  // CH1 high byte\n  buf[1] = ch1_val & 0xFF;          // CH1 low byte\n  buf[2] = (ch2_val >> 8) & 0xFF;  // CH2 high byte\n  buf[3] = ch2_val & 0xFF;          // CH2 low byte\n  for (int i = 0; i < 4; i++) {\n    TinyWireS.send(buf[i]);\n  }\n}",
      "codeUART": "// === ALTERNATIVE: UART Version (even simpler) ===\n// ATtiny85 sends 5-byte packet every 10ms via SoftwareSerial\n// Format: [0xAA, CH1_H, CH1_L, CH2_H, CH2_L]\n\n#include <SoftwareSerial.h>\n\n#define TX_PIN 1    // PB1, pin 6\n#define MEL_CH1 A3  // PB3, pin 2\n#define MEL_CH2 A2  // PB4, pin 3\n\nSoftwareSerial mySerial(-1, TX_PIN); // RX unused, TX only\n\nvoid setup() {\n  mySerial.begin(9600);\n  pinMode(MEL_CH1, INPUT);\n  pinMode(MEL_CH2, INPUT);\n}\n\nvoid loop() {\n  uint16_t ch1 = analogRead(MEL_CH1);\n  uint16_t ch2 = analogRead(MEL_CH2);\n  \n  mySerial.write(0xAA);            // Sync byte\n  mySerial.write((ch1 >> 8) & 0xFF);\n  mySerial.write(ch1 & 0xFF);\n  mySerial.write((ch2 >> 8) & 0xFF);\n  mySerial.write(ch2 & 0xFF);\n  \n  delay(10);  // 100 Hz\n}",
      "flashUsage": "~1.5 KB of 8 KB (ATtiny85 has plenty of room)",
      "ramUsage": "~64 bytes of 512 bytes"
    },
    "teensyFirmware": {
      "title": "Teensy 4.1 Firmware Changes (Base Side)",
      "language": "Arduino / C++",
      "codeI2C": "// === Add to existing ThumbJoy Teensy firmware ===\n// Replace direct analogRead of Melexis with I2C read from ATtiny85\n\n#include <Wire.h>\n\n#define GRIP_I2C_ADDR 0x27\n\nuint16_t thumbwheel_ch1 = 512;\nuint16_t thumbwheel_ch2 = 512;\n\nvoid readThumbwheel() {\n  Wire.requestFrom(GRIP_I2C_ADDR, 4);\n  if (Wire.available() == 4) {\n    uint8_t h1 = Wire.read();\n    uint8_t l1 = Wire.read();\n    uint8_t h2 = Wire.read();\n    uint8_t l2 = Wire.read();\n    thumbwheel_ch1 = (h1 << 8) | l1;  // 0–1023\n    thumbwheel_ch2 = (h2 << 8) | l2;  // 0–1023\n  }\n}\n\n// Call readThumbwheel() in your main loop instead of analogRead()\n// thumbwheel_ch1 and thumbwheel_ch2 replace the old direct reads",
      "codeUART": "// === ALTERNATIVE: UART Version ===\n// Teensy reads 5-byte packets from ATtiny85 on Serial2 (pin 7 = RX2)\n\nvoid setup() {\n  Serial2.begin(9600);  // RX2 = pin 7\n}\n\nvoid readThumbwheel() {\n  while (Serial2.available() >= 5) {\n    if (Serial2.read() == 0xAA) {  // Sync byte\n      uint8_t h1 = Serial2.read();\n      uint8_t l1 = Serial2.read();\n      uint8_t h2 = Serial2.read();\n      uint8_t l2 = Serial2.read();\n      thumbwheel_ch1 = (h1 << 8) | l1;\n      thumbwheel_ch2 = (h2 << 8) | l2;\n    }\n  }\n}",
      "linesOfCodeChange": "~15 lines added to existing firmware",
      "backwardsCompatible": "Can add #ifdef to support both direct analog and I2C/UART modes"
    }
  },

  "pcbDesign": {
    "title": "Grip PCB Layout Notes",
    "tool": "KiCad or Fusion ECAD (Alec uses Fusion)",
    "manufacturer": "JLCPCB (existing supplier)",
    "layers": 2,
    "thickness": "0.8mm (thinnest standard)",
    "dimensions": "8mm × 6mm",
    "minOrderQty": 5,
    "estimatedCost": "$2 for 5 boards + $5 shipping = $7 total",
    "layoutNotes": [
      "ATtiny85 SOIC-8 centered on board",
      "Decoupling caps on same side as ATtiny, as close to VCC/GND pins as possible",
      "Pull-up resistors on back side",
      "3 pads on one edge for shaft wires (VCC, GND, SDA) — through-hole or SMD pads",
      "2 pads on opposite edge for Melexis analog inputs (short jumper wires to sensor)",
      "Optional: 3 programming pads (MOSI, MISO, SCK) for ISP — only needed once",
      "Ground pour on bottom layer for EMI shielding from Z-axis magnet"
    ],
    "assemblyNotes": [
      "Hand-solder all components (5 SMD parts total — straightforward)",
      "Or order JLCPCB SMT assembly (~$8 extra for 5 boards fully assembled)",
      "Conformal coat entire board for moisture protection (spray-on works)",
      "Secure in grip housing with Kapton tape or small dab of silicone"
    ]
  },

  "asciiSchematic": "┌─────────────────────────────────────────────────────────┐\n│                   GRIP PCB (8mm × 6mm)                  │\n│                                                         │\n│    ┌──────────┐                                         │\n│    │ MELEXIS  │                                         │\n│    │ Hall     │──OUT1──→ Pin2 (PB3/ADC3)                │\n│    │ Sensor   │──OUT2──→ Pin3 (PB4/ADC2)                │\n│    │ (exist.) │──VCC───→ VCC rail                       │\n│    │          │──GND───→ GND rail                       │\n│    └──────────┘                                         │\n│                    ┌──────────┐                         │\n│              VCC──→│1 RESET  8│←─ VCC ←── Shaft Wire 1  │\n│                    │          │    ↑                     │\n│   Mel OUT1 ──────→│2 PB3   7│←── SCL (internal)        │\n│   Mel OUT2 ──────→│3 PB4   6│    (PB1, not used/LED)   │\n│              GND──→│4 GND   5│──→ SDA ──── Shaft Wire 3 │\n│                    │ ATtiny85 │                          │\n│                    └──────────┘                         │\n│                         ↑ ↑                             │\n│                    R1=10k R2=4.7k                       │\n│                    (reset) (SDA pullup)                  │\n│                                                         │\n│         C1=100nF ──┤├── VCC to GND                      │\n│         C2=10µF  ──┤├── VCC to GND                      │\n│                                                         │\n│    ═══ SHAFT (3 wires, 30AWG) ═══                       │\n│    Wire 1: VCC (Red)                                    │\n│    Wire 2: GND (Black)                                  │\n│    Wire 3: SDA/TX (Yellow)                              │\n└─────────────────────────────────────────────────────────┘\n                           ↓\n                    (Through HF55 hollow shaft)\n                           ↓\n┌─────────────────────────────────────────────────────────┐\n│              TEENSY 4.1 (existing ThumbJoy PCB v3)      │\n│                                                         │\n│    Wire 1 (VCC) ──→ 5V rail                             │\n│    Wire 2 (GND) ──→ GND rail                            │\n│    Wire 3 (SDA) ──→ Pin 18 (SDA) or Serial RX          │\n│                                                         │\n│    Firmware: Wire.requestFrom(0x27, 4)                  │\n│    → reads thumbwheel_ch1, thumbwheel_ch2               │\n└─────────────────────────────────────────────────────────┘",

  "testing": {
    "breadboardPrototype": {
      "title": "Step 1: Breadboard Validation",
      "time": "1-2 hours",
      "parts": [
        "1× ATtiny85 DIP-8 ($1.50)",
        "1× Sparkfun Tiny AVR Programmer ($16) or Arduino Uno as ISP",
        "2× 10kΩ potentiometers (simulate Melexis analog outputs)",
        "2× 4.7kΩ resistors (I2C pull-ups)",
        "1× 100nF capacitor",
        "1× breadboard + jumper wires",
        "1× Teensy 4.1 (or any Arduino for initial test)"
      ],
      "steps": [
        "1. Set up ATtiny85 on breadboard with 5V power",
        "2. Connect 2 pots to ADC pins (PB3, PB4) — simulate Melexis 0-5V",
        "3. Connect SDA (PB0) and SCL (PB2) to Teensy I2C bus with pull-ups",
        "4. Flash ATtiny85 with I2C slave firmware using programmer",
        "5. Flash Teensy with I2C master read code",
        "6. Open Serial Monitor — verify pot values read correctly on Teensy",
        "7. Turn pots slowly — verify smooth, glitch-free readings",
        "8. Verify latency: toggle a pin on ATtiny when reading, measure on scope"
      ],
      "successCriteria": [
        "Both channels read 0-1023 accurately (±2 counts)",
        "Latency < 5ms from analog change to Teensy read",
        "No I2C communication errors over 10 minutes",
        "Smooth tracking with no glitches or dropped packets"
      ]
    },
    "integrationTest": {
      "title": "Step 2: Integration with Real ThumbJoy",
      "time": "2-4 hours",
      "steps": [
        "1. Disconnect existing 6 Melexis wires from ThumbJoy shaft",
        "2. Dead-bug solder ATtiny85 onto small perfboard (~8×6mm)",
        "3. Connect ATtiny ADC inputs to actual Melexis sensor outputs",
        "4. Route 3 wires (VCC, GND, SDA) through HF55 shaft",
        "5. Connect to Teensy I2C bus on ThumbJoy PCB v3",
        "6. Update Teensy firmware to read from I2C instead of direct analog",
        "7. Test thumbwheel response — should feel identical to direct wiring",
        "8. Test through full Z-axis twist range — verify no EMI interference"
      ]
    },
    "productionPCB": {
      "title": "Step 3: Custom PCB",
      "time": "1-2 weeks (JLCPCB order)",
      "steps": [
        "1. Design PCB in KiCad/Fusion ECAD (8mm × 6mm, 2-layer)",
        "2. Order from JLCPCB (~$2/5pcs, or $10 with SMT assembly)",
        "3. Solder or receive assembled boards",
        "4. Flash ATtiny85 firmware",
        "5. Install in grip housing",
        "6. Conformal coat for moisture protection",
        "7. Final integration test"
      ]
    }
  },

  "riskMitigation": {
    "emInterference": {
      "risk": "Z-axis magnet may interfere with I2C signals",
      "mitigation": "I2C is digital and differential — much more noise-immune than analog. Ground pour on PCB bottom layer. If issues occur, switch to UART (single TX wire, even more robust).",
      "fallback": "UART firmware is provided as alternative — trivial to swap."
    },
    "powerBudget": {
      "risk": "ATtiny85 + Melexis may exceed shaft wire current capacity",
      "mitigation": "ATtiny85 draws ~5mA at 8MHz. Melexis draws ~10-15mA. Total ~20mA. 30AWG wire handles 860mA. Massive margin.",
      "fallback": "N/A — not a real risk."
    },
    "mechanicalFit": {
      "risk": "8mm × 6mm PCB may not fit next to Melexis bracket",
      "mitigation": "Board is thinner than a fingernail. Can be mounted vertically if needed. Dead-bug soldering also works for V1.",
      "fallback": "Reduce to 6mm × 4mm by using ATtiny412 (smaller QFN package) instead of ATtiny85 SOIC-8."
    },
    "reliability": {
      "risk": "Additional component in harsh marine environment",
      "mitigation": "ATtiny85 is industrial-rated (-40°C to +85°C). Conformal coat protects against moisture. No moving parts. MTBF is essentially infinite for this simple circuit.",
      "fallback": "Keep direct 6-wire path as backup option on PCB (add solder jumpers)."
    }
  }
}
